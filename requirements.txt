# Runtime dependencies for the Open-i MCP server and CLI.
# Install with:  pip install -r requirements.txt

mcp>=1.2.0      # Official Model Context Protocol Python SDK (provides FastMCP)
httpx>=0.27.0   # HTTP client used to call the Open-i REST API
